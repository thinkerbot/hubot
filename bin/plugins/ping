#!/bin/bash

while read -r line
do
  case "$line" in
    PING)
      echo PONG;;
    "ECHO "*)
      echo ${1:5};;
    TIME)
      date;;
    HOST)
      uname -a;;
    ERR)
      exit 1;;
  esac
done